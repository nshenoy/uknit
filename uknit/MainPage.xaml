<phone:PhoneApplicationPage
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
	xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
	xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
	xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="728" 
	x:Class="uknit.MainPage"
	FontFamily="{StaticResource PhoneFontFamilyNormal}"
	FontSize="{StaticResource PhoneFontSizeNormal}"
	Foreground="{StaticResource PhoneForegroundBrush}"
	SupportedOrientations="PortraitOrLandscape" Orientation="Portrait"
	shell:SystemTray.IsVisible="False" Margin="0">

	<!--LayoutRoot is the root grid where all page content is placed-->
	<Grid x:Name="LayoutRoot" Background="Transparent">

		<!--Panorama control-->
		<controls:Panorama Title="μknit">
			<controls:Panorama.Background>
				<ImageBrush ImageSource="SwirveDark.png" Stretch="None"/>
			</controls:Panorama.Background>

			<!--Panorama item one-->
			<controls:PanoramaItem Header="projects">
				<!--Double line list with image placeholder and text wrapping-->
				<StackPanel>
					<HyperlinkButton HorizontalAlignment="Left" Margin="12,0,0,0"  FontSize="32" NavigateUri="/Views/AddNewProject.xaml" Content="Add new project..." />
					<ListBox ItemsSource="{Binding KnittingProjects}" Margin="0,0,-12,0">
						<ListBox.ItemTemplate>
							<DataTemplate>
								<Button BorderThickness="0" Margin="0,0,0,0">
									<StackPanel Orientation="Horizontal" Margin="0,0,0,17">
										<Grid>
											<Rectangle Height="100" Width="100" Fill="{Binding RowCounterColorRGB}" Margin="12,0,9,0"/>
											<TextBlock Height="100" Width="100" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" Text="{Binding CurrentRowCount}" FontSize="72"/>
										</Grid>
										<StackPanel Width="311">
											<TextBlock Text="{Binding ProjectName}" TextWrapping="Wrap" Style="{StaticResource PhoneTextExtraLargeStyle}" />
											<TextBlock Text="{Binding ProjectDescription}" TextWrapping="Wrap" Margin="12,-6,12,0" Style="{StaticResource PhoneTextSubtleStyle}" />
										</StackPanel>
									</StackPanel>
									<toolkit:ContextMenuService.ContextMenu>
										<toolkit:ContextMenu>
											<toolkit:MenuItem Header="edit project" Tag="{Binding ProjectName}" Click="OnContextMenuClick_EditProject" />
										</toolkit:ContextMenu>
									</toolkit:ContextMenuService.ContextMenu>
								</Button>
							</DataTemplate>
						</ListBox.ItemTemplate>
					</ListBox>
				</StackPanel>
			</controls:PanoramaItem>
			<controls:PanoramaItem Header="converter">
				<Grid>
					<Rectangle Fill="Black" Opacity=".4" Margin="0,0,0,207" />
					<StackPanel x:Name="ContentPanel" Margin="0,0,0,207" d:LayoutOverrides="GridBox">
						<Grid x:Name="ConvertGrid">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto" />
								<RowDefinition Height="*" />
							</Grid.RowDefinitions>
							<TextBox x:Name="Yards" Grid.Row="0" Grid.Column="0" Text="0" MaxLength="5" TextAlignment="Right" GotFocus="Yards_GotFocus" LostFocus="Yards_LostFocus" InputScope="Digits"/>
							<TextBlock x:Name="YardsLabel" Grid.Row="0" Grid.Column="1" Text="yards" Foreground="WhiteSmoke" Padding="10" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource PhoneTextLargeStyle}" />
							<TextBox x:Name="Meters" Grid.Row="1" Grid.Column="0" Text="0" MaxLength="5" TextAlignment="Right" GotFocus="Meters_GotFocus" LostFocus="Meters_LostFocus" InputScope="Digits" />
							<TextBlock x:Name="MetersLabel" Grid.Row="1" Grid.Column="1" Text="meters" Foreground="WhiteSmoke" Padding="10" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource PhoneTextLargeStyle}" />
						</Grid>
						<Button x:Name="Convert" Content="Convert" Click="Convert_Click"/>
					</StackPanel>
				</Grid>
			</controls:PanoramaItem>

			<!--Panorama item two-->
		</controls:Panorama>
	</Grid>

	<phone:PhoneApplicationPage.ApplicationBar>
		<shell:ApplicationBar IsVisible="True"  IsMenuEnabled="True">
			<shell:ApplicationBar.MenuItems>
				<shell:ApplicationBarMenuItem Text="settings" Click="OnClick_Settings"/>
			</shell:ApplicationBar.MenuItems>
		</shell:ApplicationBar>
	</phone:PhoneApplicationPage.ApplicationBar>
</phone:PhoneApplicationPage>
